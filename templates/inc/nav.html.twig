<nav>
    <ul>
        <li><a href="{{ path('app_home') }}" title="Home page">Home</a></li>
        <li><a href="{{ path('app_serie_list') }}" title="All Series">All series !</a></li>

        {% if app.user %}

        <li><a href="{{ path('app_serie_create') }}" title="Create a new serie">Create a new serie</a></li>
        <li><a href="{{ path('app_season_create') }}" title="Create a new season">Create a new season</a></li>
        <li><a href="{{ path('app_logout') }}" title="Logout">Logout ({{ app.user.firstName }})</a></li>

            {% if is_granted("ROLE_ADMIN") %}
            <li><a href="{{ path('app_admin_dashboard') }}" title="Admin Dashboard">Dashboard ({{ app.user.firstName }})</a></li>
            {% endif %}
        {% else %}

        <li><a href="{{ path('app_login') }}" title="Login">Login</a></li>
        <li><a href="{{ path('app_register') }}" title="Register">Register</a></li>

        {% endif %}

    </ul>
</nav>